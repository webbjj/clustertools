version: 2

sphinx:
  configuration: docs/source/conf.py

python:
  version: 3
  install:
    - method: setuptools
      path: .
    - requirements: docs/requirements.txt

python:
  use_system_site_packages: true
